# دليل الاستعمال — مساعد الدروس

تطبيق يجيب تلاميذك عن أسئلتهم انطلاقًا من **دروسك أنت**، وليس من الإنترنت.

يصل إليه التلاميذ بطريقتين:

- **تطبيق ويب**: تشارك معهم رابطًا واحدًا (في مجموعة الواتساب مثلًا). يفتحونه في المتصفح، ويمكنهم تثبيته في شاشة الهاتف كأي تطبيق. بدون تحميل وبدون حساب.
- **بوت تيليغرام**: لمن يفضّل الدردشة داخل تطبيق مراسلة.

ولك أنت **لوحة الأستاذ**: ترى فيها ما يسأله التلاميذ فعلًا، والأهم: الأسئلة التي لا تجيب عنها دروسك.

---

## لماذا لم نستعمل الواتساب مباشرة؟

لأن شركة Meta تقف بينك وبين تلاميذك، والانتظار لا يستحق العناء.

واجهة الواتساب الرسمية (WhatsApp Business API) تتطلب حساب Meta Business، ورقم هاتف **غير مسجَّل أصلًا في الواتساب**، ثم التحقق من الحساب قبل أن تتمكن من مراسلة قسم حقيقي. الرقم التجريبي المجاني الذي تمنحه Meta يصل إلى 5 أرقام فقط. وحتى بعد الموافقة، لا يمكنك الرد إلا داخل 24 ساعة من رسالة التلميذ.

هذا المشروع يتجاوز كل ذلك. يبقى الواتساب هو **المكان** الذي تتواصل فيه مع تلاميذك — أنت فقط تشارك رابطًا في المجموعة بدل تمرير الرسائل عبر Meta.

وإذا حصلت على الموافقة لاحقًا، لن يضيع شيء: عقل التطبيق (`src/tutor.js`) منفصل عن قنوات التواصل (`src/server.js` و `src/telegram.js`). إضافة الواتساب تعني كتابة ملف واحد جديد، لا إعادة بناء المشروع.

---

## ما تحتاجه قبل البدء

1. حاسوب (ويندوز أو ماك أو لينكس).
2. برنامج [Node.js](https://nodejs.org) نسخة 20 أو أحدث. حمّله وثبّته بشكل عادي.
3. مفتاح API من [console.anthropic.com](https://console.anthropic.com) — وهذا ما يؤدي ثمن الأجوبة.

> جميع الأوامر المكتوبة في هذا الدليل تُكتب في **Terminal** (في الماك ولينكس) أو **PowerShell** (في ويندوز)، بعد الدخول إلى مجلد المشروع.

---

## الخطوة 1: التثبيت

افتح مجلد المشروع في الطرفية ثم اكتب:

```bash
npm install
```

تنتظر قليلًا حتى ينتهي. تفعل هذا مرة واحدة فقط.

---

## الخطوة 2: مفتاح API

1. ادخل إلى [console.anthropic.com](https://console.anthropic.com).
2. أنشئ حسابًا، ثم اذهب إلى **API Keys** وأنشئ مفتاحًا جديدًا.
3. انسخ المفتاح واحتفظ به — لن يظهر لك مرة أخرى.
4. أضف رصيدًا إلى الحساب (Billing). بدون رصيد لن يعمل التطبيق.

---

## الخطوة 3: الإعدادات

انسخ ملف الإعدادات النموذجي:

```bash
cp .env.example .env
```

ثم افتح الملف `.env` بأي محرر نصوص، واملأ على الأقل هذه السطور:

```ini
ANTHROPIC_API_KEY=sk-ant-...
SUBJECT=Sciences de la Vie et de la Terre (SVT)
TEACHER_NAME=الأستاذ عماري
CLASS_CODE=svt2026
TEACHER_PASSWORD=كلمة-سر-تعرفها-أنت-وحدك
```

شرح أهم الإعدادات:

| الإعداد | ما يفعله |
|---|---|
| `ANTHROPIC_API_KEY` | مفتاحك من الخطوة 2 |
| `SUBJECT` | المادة التي تدرّسها. يمكنك كتابتها بالعربية: `علوم الحياة والأرض` |
| `TEACHER_NAME` | اسمك كما يظهر للتلاميذ |
| `BOT_NAME` | اسم المساعد، مثلًا `مساعد علوم الحياة والأرض` |
| `DEFAULT_LANG` | `ar` للعربية دائمًا، `fr` للفرنسية، أو `auto` ليجيب بلغة سؤال التلميذ |
| `CLASS_CODE` | رمز يكتبه التلميذ مرة واحدة فقط |
| `TEACHER_PASSWORD` | كلمة سر لوحة الأستاذ |
| `RATE_LIMIT_PER_HOUR` | أقصى عدد أسئلة للتلميذ الواحد في الساعة (40 افتراضيًا) |

> **مهم جدًا: اضبط `CLASS_CODE`.**
> بدونه، كل من يحصل على الرابط يستطيع طرح أسئلة بلا حدود **على حسابك أنت**. مع الرمز، يكتبه التلميذ مرة واحدة ولا يُطلب منه بعدها.

---

## الخطوة 4: إضافة دروسك

ضع ملفات دروسك داخل مجلد `lessons/`:

```
lessons/
  01-la-cellule.pdf
  02-la-mitose.pdf
  semestre-2/03-geologie.pptx
```

الصيغ المقبولة:

| الصيغة | ملاحظات |
|---|---|
| `.pdf` | رقمية **وكذلك المصوّرة بالماسح الضوئي أو بالهاتف**، بما فيها الكتابة اليدوية |
| `.docx` | ملفات Word |
| `.pptx` | عروض PowerPoint، مع ملاحظات العارض |
| `.md` / `.txt` | ملاحظات نصية بسيطة |

نصائح:

- **سمِّ الملفات بوضوح.** اسم الملف هو اسم الدرس الذي يراه التلميذ: `02-la-mitose.pdf` يظهر كـ *02 la mitose*.
- **المجلدات الفرعية تعمل**: `semestre-2/03-geologie.pdf` يظهر كـ *semestre-2 / 03 geologie*.
- **درس واحد في كل ملف** هو الأفضل، ويبقي حجم كل ملف تحت 32 ميغابايت.

### ملاحظة خاصة بمادتك

دروس علوم الحياة والأرض مليئة بالرسوم والتخطيطات (schémas, coupes géologiques, cycles). التطبيق يقرأ هذه الرسوم وينقل **كل البيانات المكتوبة عليها** (les légendes)، لأنها هي المحتوى الحقيقي للدرس وليست زينة.

لكي يكون هذا ناجحًا:

- إذا صوّرت صفحة بهاتفك، تأكد أن **أسماء الأجزاء على التخطيط مقروءة**. صورة واضحة تعني جوابًا صحيحًا.
- الرسوم بدون بيانات مكتوبة لن يفهمها التطبيق جيدًا. إن كان لديك تخطيط مهم بدون بيانات، أضف شرحه في ملف `.txt` بجانبه.

---

## الخطوة 5: قراءة الدروس

```bash
npm run ingest
```

هنا يقرأ التطبيق ملفاتك ويحوّلها إلى فهرس قابل للبحث. ملفات PDF تُرسَل إلى Claude ليقرأها — وهو يتعامل مع الصفحات الممسوحة والخط اليدوي والحروف العربية والرسوم والتخطيطات.

**تدفع ثمن قراءة كل ملف مرة واحدة فقط.** النتيجة محفوظة، فإذا أعدت تشغيل الأمر لن يُعاد قراءة الملفات التي لم تتغير.

أعد تشغيل هذا الأمر كلما أضفت درسًا جديدًا أو عدّلت درسًا.

---

## الخطوة 6: تشغيل التطبيق

```bash
npm start
```

ستظهر لك رسالة فيها رابطان:

- `http://localhost:3000` — هذا ما يراه التلاميذ.
- `http://localhost:3000/teacher` — لوحتك أنت.

للدخول إلى اللوحة: اكتب أي اسم مستخدم، وكلمة السر هي `TEACHER_PASSWORD` التي وضعتها في ملف `.env`.

> في هذه المرحلة التطبيق يعمل **على حاسوبك فقط**. لمشاركته مع التلاميذ، انظر قسم *نشر التطبيق على الإنترنت* أسفله.

---

## لوحة الأستاذ

تعرض لك:

- **عدد الأسئلة** وعدد التلاميذ الذين استعملوا التطبيق.
- **الأسئلة حسب الدرس**: أي درس يطرح حوله التلاميذ أكبر عدد من الأسئلة — غالبًا هو الدرس الأصعب عليهم.
- **أسئلة لا تغطيها دروسك**: هذه هي أهم قائمة في اللوحة. كل سؤال هنا لم يجد التطبيق جوابه في موادك، فأحال التلميذ عليك. اقرأها قبل الحصة القادمة.
- **آخر الأسئلة**: اضغط على أي سؤال لترى الجواب كاملًا.

---

## بوت تيليغرام (اختياري)

1. افتح تيليغرام وابحث عن [@BotFather](https://t.me/botfather).
2. أرسل له `/newbot`، ثم اتبع تعليماته (سيطلب اسمًا للبوت، ثم اسم مستخدم ينتهي بـ `bot`).
3. سيعطيك رمزًا (token) بهذا الشكل: `123456:ABC-DEF...`.
4. ضع الرمز في ملف `.env`:

```ini
TELEGRAM_BOT_TOKEN=123456:ABC-DEF...
```

5. في نافذة طرفية **ثانية** (اترك `npm start` تعمل في الأولى):

```bash
npm run telegram
```

سيطبع لك رابطًا من الشكل `https://t.me/...` — هذا ما تشاركه مع التلاميذ.

الأوامر المتاحة للتلميذ داخل البوت:

| الأمر | ما يفعله |
|---|---|
| `/start` | بداية المحادثة |
| `/lessons` | اختيار درس واحد للتركيز عليه |
| `/all` | البحث في كل الدروس من جديد |
| `/reset` | بدء محادثة جديدة |
| `/help` | عرض المساعدة |

---

## نشر التطبيق على الإنترنت

ليحصل التلاميذ على رابط حقيقي يعمل في أي وقت، تحتاج إلى استضافة. خدمات مثل [Railway](https://railway.app) أو [Render](https://render.com) أو [Fly.io](https://fly.io) تكفي، ولها خطط مجانية أو شبه مجانية.

الخطوات العامة:

1. ارفع المشروع إلى GitHub واربطه بالخدمة.
2. أضف نفس متغيرات ملف `.env` في إعدادات الخدمة (Environment Variables).
3. **مهم**: شغّل `npm run ingest` قبل النشر، وأضف الملف الناتج `data/index.json` إلى المستودع — أو اجعل الخدمة تشغّل هذا الأمر عند النشر. بدون هذا الملف لن يجيب التطبيق عن أي سؤال.

بعدها شارك الرابط في مجموعة القسم. يستطيع التلاميذ في أندرويد والآيفون الضغط على *إضافة إلى الشاشة الرئيسية* ليصبح كتطبيق مثبَّت.

---

## التكلفة

تدفع لشركة Anthropic حسب الاستعمال. هناك تكلفتان:

**قراءة الدروس** — مرة واحدة فقط ومحفوظة إلى الأبد. ملف PDF من 30 صفحة يكلف سنتيمات قليلة.

**الأجوبة** — هذه هي التكلفة المتكررة، وتتعلق بالنموذج المختار في `CLAUDE_MODEL`:

| النموذج | مناسب لـ | التكلفة |
|---|---|---|
| `claude-opus-5` (الافتراضي) | الرياضيات والفيزياء والاستدلال المعقّد | الأعلى |
| `claude-sonnet-5` | أغلب المواد | متوسطة |
| `claude-haiku-4-5` | المواد القائمة على الفهم والتعاريف والآليات | الأقل |

### توصيتي لمادتك

مادة علوم الحياة والأرض قائمة أساسًا على التعاريف والآليات وقراءة الوثائق، لا على حسابات متعددة المراحل. لذلك:

```ini
CLAUDE_MODEL=claude-haiku-4-5
CLAUDE_EFFORT=low
```

هذا سيقلّل فاتورتك بشكل كبير، والجودة ستكون كافية جدًا لمادتك. ابدأ به، وراقب لوحة الأستاذ أسبوعًا. إن لاحظت أن الأجوبة سطحية في درس معيّن، غيّر إلى `claude-sonnet-5`. تغيير سطر واحد في `.env` ثم إعادة التشغيل، بدون تعديل أي كود.

عاملان يحميان فاتورتك أصلًا:

- **`RATE_LIMIT_PER_HOUR`** يحدّد عدد أسئلة التلميذ الواحد في الساعة.
- **الذاكرة المؤقتة (cache)**: إن كانت دروسك صغيرة بما يكفي، تُرسَل كاملة مع كل سؤال لكنها محفوظة مؤقتًا، فتكلف الأسئلة المتكررة جزءًا يسيرًا من الأولى.

---

## كيف يجيب التطبيق؟

التطبيق مقيَّد عمدًا:

- **يجيب من دروسك، لا من الإنترنت.** إن كنت تدرّس تعريفًا أو منهجية تختلف عن المتداول، درسك هو المرجع.
- **إن لم تغطِّ موادك السؤال، يقول ذلك بوضوح** ويحيل التلميذ عليك، بدل أن يخترع جوابًا. وتظهر هذه الأسئلة في لوحتك.
- **لا يحل الواجبات.** إن طلب التلميذ حل تمرين، يذكّره بالقاعدة المناسبة، ويشرح الخطوة الأولى، ثم يطلب منه إكمالها بنفسه، ويصحّح له محاولته. إن أردت أن يعطي الحل كاملًا، ضع `HINT_MODE=false`.
- **يجيب بلغة التلميذ**: إن سأل بالعربية أجاب بالعربية، وإن سأل بالفرنسية أجاب بالفرنسية.

---

## رسالة جاهزة لإرسالها إلى مجموعة القسم

انسخ هذه الرسالة وعدّل الرابط والرمز:

```
السلام عليكم،

وضعت لكم مساعدًا يجيب عن أسئلتكم في مادة علوم الحياة والأرض،
انطلاقًا من الدروس التي شرحناها في القسم.

الرابط: (ضع الرابط هنا)
رمز القسم: svt2026

اكتبوا الرمز مرة واحدة فقط في أول مرة.
اسألوا بالعربية أو بالفرنسية، كما تشاؤون.

ملاحظتان:
- المساعد لا يحل لكم التمارين، بل يوجّهكم خطوة بخطوة.
- إن سألتم عن درس لم ندرسه بعد، سيقول لكم ذلك.

لإضافته إلى شاشة هاتفكم: افتحوا الرابط، ثم اختاروا
"إضافة إلى الشاشة الرئيسية" من قائمة المتصفح.
```

---

## مشاكل شائعة وحلولها

| المشكلة | الحل |
|---|---|
| `No lessons indexed yet` عند التشغيل | لم تشغّل `npm run ingest` بعد، أو مجلد `lessons/` فارغ |
| `No lesson files found` | ضع ملفاتك داخل مجلد `lessons/` وتأكد أن صيغتها مقبولة |
| رسالة خطأ عند طرح سؤال | تحقق من `ANTHROPIC_API_KEY` في `.env`، ومن وجود رصيد في حسابك |
| التطبيق لا يقبل رمز القسم | الرمز في `.env` يجب أن يطابق ما يكتبه التلميذ (حالة الأحرف غير مهمة) |
| PDF كبير جدًا | الحد الأقصى 32 ميغابايت. قسّم الملف إلى فصول |
| أضفت درسًا ولم يظهر | أعد تشغيل `npm run ingest` ثم أعد تشغيل التطبيق، أو اضغط *Reload lessons* في لوحة الأستاذ |
| البوت لا يرد في تيليغرام | تأكد أن `npm run telegram` ما زال يعمل في نافذة الطرفية |

---

## أين توجد الأشياء في المشروع

```
lessons/            ملفات دروسك
data/               الفهرس وسجل المحادثات
src/
  ingest.js         يقرأ الدروس ويبني الفهرس
  extract/          قراءة pdf و docx و pptx والنصوص
  tutor.js          عقل التطبيق: التعليمات والأجوبة
  server.js         تطبيق الويب
  telegram.js       بوت تيليغرام
public/             واجهة التلاميذ
```
